const Wrapper=(props)=>{
    return props.children//It will return all the children and acts as a wrapper instead of using multiple div
    //this acts as one root component rendered and not using any kind of DOM 
}
export default Wrapper